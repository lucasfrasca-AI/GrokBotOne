--- /workspace/context/plan/v002.md	2026-09-06 23:06:12.641689871 +0000
+++ /workspace/context/plan/v003.md	2026-09-07 07:10:27.404051890 +0000
@@ -1,21 +1,22 @@
-# Learning Plan — v002
+# Learning Plan — v003
 
 - **Written:** 2026-09-07 (AEST)
 - **Owner:** Curriculum (reports to Mentor)
-- **Prior:** v001
-- **Status:** active — seeded from competencies.md v0 (no Analyst signals yet)
+- **Prior:** v002
+- **Status:** active — rebuilt competency IDs; Lucas evidence+target final; one Analyst run (`20260906T231515Z`)
 
 ## Rationale
 
-Competencies landed with provisional evidence. Largest gaps are `poc-ship` (1→4), then `workshop` and `market-fit` (1→3), then the discovery/scoping/design cluster (2→4). v002 replaces the empty board with a 30/70 study/build slate aimed at those gaps. Integration and security-deploy stay maintenance (already at 3). Targets will reshuffle when Analyst flags the same gap across two runs, or on the Sunday rewrite.
+Hold lifted after Lucas filled evidence and targets on the rebuilt matrix. v002's retired IDs (`poc-ship`, `market-fit`, `workshop`, etc.) are gone. v003 maps a 30/70 study/build board to Lucas's real gaps, weighted toward Analyst flags from the first Reddit run (demand ≥ 4 and evidence ≤ 2): `api-auth`, `cust-discovery`, `ambiguity-decomp`, `exec-comm`, `legacy-data`. Hard-gate production gaps (`prod-ship`, `prod-ownership`, `codebase-debug`) stay on the board even without demand signal this run — they are the bar for FDE credibility. Only one Analyst run exists, so the two-run persistence rewrite rule has not fired; Sunday rewrite resumes for standing refresh.
 
 ## Operating rules
 
 1. Hold a **30% study / 70% build** split across active targets.
 2. Never edit a written plan version. Always add a new numbered version with a diff against the prior version and a one-paragraph rationale.
-3. Canonical latest copy: `/workspace/context/plan.md`. Snapshots: `/workspace/context/plan/vNNN.md`.
+3. Canonical latest: `/workspace/context/plan.md`. Snapshots: `/workspace/context/plan/vNNN.md` (+ `.diff`).
 4. Mentor picks **one** daily target from this board. Curriculum does not set the day-of pick.
-5. Each new version goes to Mentor, and to Herald once that bot exists (Phase 5).
+5. Each new version goes to Mentor and Herald.
+6. Evidence and targets are Lucas-only — never invent or overwrite scores when rewriting.
 
 ## Active targets
 
@@ -23,51 +24,68 @@
 
 | priority | id | evidence→target | build artifact |
 |----------|-----|-----------------|----------------|
-| P0 | poc-ship | 1→4 | Thin POC→prod playbook: cutover, rollback, ownership, exit criteria for one sample security→FDE engagement |
-| P1 | cust-discovery | 2→4 | One structured discovery script + worked example that separates symptom from constraint |
-| P1 | scope-write | 2→4 | Turn that discovery into a scoped SoW (outcomes, out-of-scope, risks, success checks) |
-| P2 | sol-design | 2→4 | Before/after architecture sketch for the same engagement |
-| P2 | write-up | 2→4 | Decision-log + handoff runbook template customers can keep |
+| P0 | prod-ship | 2 → 4-5 | Ship one small system and keep it running under real use (debug + iterate, not a disposable demo) |
+| P0 | prod-ownership | 1 → 5 | Own that system through at least one failure cycle; write the incident/decision log |
+| P0 | cust-discovery | 1 → 4 | Structured discovery script + one worked example that surfaces the real problem behind the stated one |
+| P0 | ambiguity-decomp | 2 → 4 | Turn one ambiguous customer-style brief into a concrete plan (interview-grade decomp) |
+| P1 | api-auth | 2 → 5 | Integrate a real API with production-grade auth (not a toy curl script) |
+| P1 | legacy-data | 1 → 4 | Small ETL or migration against a messy/legacy-shaped data source |
+| P1 | scope-write | 2 → 5 | Scoped engagement artifact from the discovery/decomp work (outcomes, out-of-scope, risks, success checks) |
+| P2 | eval-design | 2 → 5 | Eval suite that catches hallucination/regression for one LLM path |
+| P2 | llm-integrate | 2-3 → 5 | Wire an LLM into the live system from `prod-ship` (not a notebook demo) |
 
 ### Study — 30%
 
 | priority | id | evidence→target | study focus |
 |----------|-----|-----------------|-------------|
-| P0 | market-fit | 1→3 | Read collector captures once Pulse/Lurk land; note which FDE skills employers buy this quarter |
-| P1 | workshop | 1→3 | Study 2–3 strong technical workshop/demo formats (Scope on demand if a specific video is worth it) |
-| P2 | stakeholder | 2→4 | Short read on exec-vs-IC communication patterns; fold into discovery script |
+| P0 | exec-comm | 1 → 5 | How strong FDEs/SEs explain technical tradeoffs to skeptical non-technical stakeholders; fold into discovery and scope write-ups |
+| P1 | prod-lang | 1-2 → 4-5 | Deliberate Python depth (AI-adjacent baseline) — not syntax tourism |
+| P1 | codebase-debug | 1 → 4 | Method for debugging unfamiliar codebases; pair with timed sessions on a repo you did not write |
+| P2 | enterprise-auth | 2 → 4 | OIDC/SAML patterns in customer environments |
+| P2 | context-eng | 3 → 5 | Deepen RAG/embeddings/memory beyond current comfort (already strongest Applied AI score) |
 
 ### Maintenance (light touch)
 
 | id | evidence→target | note |
 |----|-----------------|------|
-| integration | 3→4 | Practice only when a build artifact needs an API/auth path |
-| security-deploy | 3→4 | Strength to lean on inside POC and workshop builds — not a primary lane |
+| cloud-deploy | 2-3 → 4-5 | Use when a build needs real cloud deploy — not a primary lane yet |
+| agent-orch | 3 → 5* | Strength to lean on inside `llm-integrate` / harness work |
+| compliance-deploy | 3 → 5 | Strength from security background — apply inside customer-shaped builds |
 
 ## Suggested sequencing (Mentor day picks)
 
-1. `poc-ship` build (P0) — several days
-2. `cust-discovery` build → immediately `scope-write` build (paired)
-3. Interleave `market-fit` study when captures appear
-4. `sol-design` + `write-up` once scoping exists
-5. `workshop` study before any demo artifact
+1. `prod-ship` build — establish the live system (multi-day)
+2. Interleave `cust-discovery` + `ambiguity-decomp` builds early (flagged, interview-weighted)
+3. `api-auth` and `legacy-data` builds against that system or a customer-shaped sandbox
+4. `scope-write` once discovery/decomp exists
+5. `exec-comm` study continuously; practice inside every write-up
+6. `prod-ownership` when the live system has something real to fail
+7. `llm-integrate` + `eval-design` once the production substrate exists
+8. `prod-lang` / `codebase-debug` study as standing depth work
 
 ## Study / build allocation
 
 | Lane | Share | Current load |
 |------|-------|--------------|
-| Study | 30% | market-fit, workshop, stakeholder |
-| Build | 70% | poc-ship, cust-discovery, scope-write, sol-design, write-up |
+| Study | 30% | exec-comm, prod-lang, codebase-debug, enterprise-auth, context-eng |
+| Build | 70% | prod-ship, prod-ownership, cust-discovery, ambiguity-decomp, api-auth, legacy-data, scope-write, eval-design, llm-integrate |
 
-## Open dependencies
+## Analyst flags in play (one run only)
 
-- No `/workspace/context/signals.md` yet — gap persistence rule cannot fire until Analyst runs twice
-- Captures dirs empty — `market-fit` study waits on Pulse/Lurk
-- Herald not live (Phase 5) — versions notify Mentor only until then
-
-## Diff vs v001
-
-- Replaced empty target board with prioritized build/study table from competencies.md gaps
-- Added Mentor daily-pick rule and suggested sequencing
-- Added maintenance lane for integration / security-deploy
-- Documented dependencies on signals, captures, and Herald
+| id | demand | evidence | persistent across 2 runs? |
+|----|--------|----------|---------------------------|
+| api-auth | 4 | 2 | no — first run |
+| cust-discovery | 4 | 1 | no — first run |
+| ambiguity-decomp | 4 | 2 | no — first run |
+| exec-comm | 4 | 1 | no — first run |
+| legacy-data | 4 | 1 | no — first run |
+
+Rewrite on persistence still waits for a second confirming Analyst run.
+
+## Diff vs v002
+
+- Replaced retired v0 IDs with rebuilt matrix (`prod-ship`, `cust-discovery`, Applied AI, Integration Wall, Customer-facing set)
+- Targets now use Lucas-final evidence/target, not Mentor seed scores
+- Prioritized Analyst flags from run `20260906T231515Z`
+- Dropped fabricated `poc-ship` / `market-fit` / `workshop` / `stakeholder` / `sol-design` / `write-up` board
+- Restored hard-gate production ownership and codebase-debug despite thin demand signal this run
